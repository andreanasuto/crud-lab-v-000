import React, { Component } from 'react';

class Restaurants extends Component {
  const restaurants = 
  render() {
    return(
      <ul>
        {restaurants}
      </ul>
    );
  }
};

export default Restaurants;
